From 4d87f46333365c23f9501c14d380ead5bd37aacc Mon Sep 17 00:00:00 2001
From: Stefan Lippers-Hollmann <s.l-h@gmx.de>
Date: Sat, 22 Mar 2014 03:03:18 +0100
Subject: [PATCH 420/456] rtl8192du: drop unused function rtw_atoi()

Signed-off-by: Stefan Lippers-Hollmann <s.l-h@gmx.de>
---
 include/osdep_service.h |  3 ---
 os_dep/osdep_service.c  | 21 ---------------------
 2 files changed, 24 deletions(-)

--- a/drivers/staging/rtl8192du/include/osdep_service.h
+++ b/drivers/staging/rtl8192du/include/osdep_service.h
@@ -199,11 +199,8 @@ u32	rtw_systime_to_ms(u32 systime);
 u32	rtw_ms_to_systime(u32 ms);
 
 void	rtw_sleep_schedulable(int ms);
-
 void	rtw_usleep_os(int us);
 
-u32	rtw_atoi(u8* s);
-
 static inline unsigned char _cancel_timer_ex(struct timer_list *timer)
 {
 	return del_timer_sync(timer);
--- a/drivers/staging/rtl8192du/os_dep/osdep_service.c
+++ b/drivers/staging/rtl8192du/os_dep/osdep_service.c
@@ -36,27 +36,6 @@ inline int RTW_STATUS_CODE(int error_cod
 	}
 }
 
-u32 rtw_atoi(u8* s)
-{
-
-	int num=0,flag=0;
-	int i;
-	for (i=0;i<=strlen(s);i++)
-	{
-	  if (s[i] >= '0' && s[i] <= '9')
-		 num = num * 10 + s[i] -'0';
-	  else if (s[0] == '-' && i==0)
-		 flag =1;
-	  else
-		  break;
-	 }
-
-	if (flag == 1)
-	   num = num * -1;
-
-	 return(num);
-}
-
 inline u8* _rtw_vmalloc(u32 sz)
 {
 	u8	*pbuf;
