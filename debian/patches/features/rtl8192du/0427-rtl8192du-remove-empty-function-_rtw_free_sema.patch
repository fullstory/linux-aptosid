From 5129a90bfe7346b803f5acfe76ab26c29b02990a Mon Sep 17 00:00:00 2001
From: Stefan Lippers-Hollmann <s.l-h@gmx.de>
Date: Sat, 22 Mar 2014 03:03:25 +0100
Subject: [PATCH 427/456] rtl8192du: remove empty function _rtw_free_sema()

Signed-off-by: Stefan Lippers-Hollmann <s.l-h@gmx.de>
---
 core/rtw_cmd.c          | 2 --
 core/rtw_xmit.c         | 2 --
 include/osdep_service.h | 1 -
 include/rtw_pwrctrl.h   | 1 -
 os_dep/osdep_service.c  | 4 ----
 5 files changed, 10 deletions(-)

--- a/drivers/staging/rtl8192du/core/rtw_cmd.c
+++ b/drivers/staging/rtl8192du/core/rtw_cmd.c
@@ -88,8 +88,6 @@ void _rtw_free_evt_priv(struct evt_priv
 void _rtw_free_cmd_priv(struct cmd_priv *pcmdpriv)
 {
 	if (pcmdpriv) {
-		_rtw_free_sema(&(pcmdpriv->cmd_queue_sema));
-		_rtw_free_sema(&(pcmdpriv->terminate_cmdthread_sema));
 		kfree(pcmdpriv->cmd_allocated_buf);
 		kfree(pcmdpriv->rsp_allocated_buf);
 	}
--- a/drivers/staging/rtl8192du/core/rtw_xmit.c
+++ b/drivers/staging/rtl8192du/core/rtw_xmit.c
@@ -212,8 +212,6 @@ exit:
 
 static void  rtw_mfree_xmit_priv_lock(struct xmit_priv *pxmitpriv)
 {
-	_rtw_free_sema(&pxmitpriv->xmit_sema);
-	_rtw_free_sema(&pxmitpriv->terminate_xmitthread_sema);
 }
 
 void _rtw_free_xmit_priv(struct xmit_priv *pxmitpriv)
--- a/drivers/staging/rtl8192du/include/osdep_service.h
+++ b/drivers/staging/rtl8192du/include/osdep_service.h
@@ -178,7 +178,6 @@ enum {
 
 #define rtw_update_mem_stat(flag, sz) do {} while (0)
 
-void	_rtw_free_sema(struct  semaphore *sema);
 void	_rtw_up_sema(struct  semaphore *sema);
 u32	_rtw_down_sema(struct  semaphore *sema);
 void	_rtw_mutex_init(_mutex *pmutex);
--- a/drivers/staging/rtl8192du/include/rtw_pwrctrl.h
+++ b/drivers/staging/rtl8192du/include/rtw_pwrctrl.h
@@ -108,7 +108,6 @@ static inline void _init_pwrlock(struct
 
 static inline void _free_pwrlock(struct  semaphore *plock)
 {
-	_rtw_free_sema(plock);
 }
 
 
--- a/drivers/staging/rtl8192du/os_dep/osdep_service.c
+++ b/drivers/staging/rtl8192du/os_dep/osdep_service.c
@@ -36,10 +36,6 @@ inline int RTW_STATUS_CODE(int error_cod
 	}
 }
 
-void _rtw_free_sema(struct  semaphore *sema)
-{
-}
-
 void _rtw_up_sema(struct  semaphore *sema)
 {
 	up(sema);
