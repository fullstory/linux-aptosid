From 13c138cf54bc19e07ee53e6160e2ba05e4306dc7 Mon Sep 17 00:00:00 2001
From: Larry Finger <Larry.Finger@lwfinger.net>
Date: Fri, 26 Apr 2013 19:10:57 -0500
Subject: [PATCH 063/390] Convert "typedef enum" for rtw_led.h

Signed-off-by: Larry Finger <Larry.Finger@lwfinger.net>
---
 hal/rtl8192du_led.c | 28 ++++++++++++++--------------
 include/rtw_led.h   | 36 ++++++++++++++++++------------------
 2 files changed, 32 insertions(+), 32 deletions(-)

--- a/drivers/staging/rtl8192du/hal/rtl8192du_led.c
+++ b/drivers/staging/rtl8192du/hal/rtl8192du_led.c
@@ -1301,12 +1301,12 @@ void BlinkWorkItemCallback(struct work_s
 void
 SwLedControlMode0(
 	_adapter		*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 );
 void
 SwLedControlMode0(
 	_adapter		*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 )
 {
 	struct led_priv	*ledpriv = &(padapter->ledpriv);
@@ -1423,12 +1423,12 @@ SwLedControlMode0(
 void
 SwLedControlMode1(
 	_adapter		*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 );
 void
 SwLedControlMode1(
 	_adapter		*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 )
 {
 	HAL_DATA_TYPE		*pHalData = GET_HAL_DATA(padapter);
@@ -1699,12 +1699,12 @@ SwLedControlMode1(
 void
 SwLedControlMode2(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 );
 void
 SwLedControlMode2(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 )
 {
 	struct led_priv	 *ledpriv = &(padapter->ledpriv);
@@ -1869,12 +1869,12 @@ SwLedControlMode2(
  void
  SwLedControlMode3(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 );
  void
  SwLedControlMode3(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 )
 {
 	struct led_priv	*ledpriv = &(padapter->ledpriv);
@@ -2053,12 +2053,12 @@ SwLedControlMode2(
 void
 SwLedControlMode4(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 );
 void
 SwLedControlMode4(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 )
 {
 	struct led_priv	*ledpriv = &(padapter->ledpriv);
@@ -2390,12 +2390,12 @@ SwLedControlMode4(
 void
 SwLedControlMode5(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 );
 void
 SwLedControlMode5(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 )
 {
 	HAL_DATA_TYPE	*pHalData = GET_HAL_DATA(padapter);
@@ -2486,12 +2486,12 @@ SwLedControlMode5(
 void
 LedControl871x(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 	);
 void
 LedControl871x(
 	_adapter				*padapter,
-	LED_CTL_MODE		LedAction
+	enum LED_CTL_MODE		LedAction
 	)
 {
 	struct led_priv	*ledpriv = &(padapter->ledpriv);
--- a/drivers/staging/rtl8192du/include/rtw_led.h
+++ b/drivers/staging/rtl8192du/include/rtw_led.h
@@ -24,23 +24,23 @@
 #include <osdep_service.h>
 #include <drv_types.h>
 
-#define MSECS(t)        (HZ * ((t) / 1000) + (HZ * ((t) % 1000)) / 1000)
+#define MSECS(t)	(HZ * ((t) / 1000) + (HZ * ((t) % 1000)) / 1000)
 
-typedef enum _LED_CTL_MODE{
-        LED_CTL_POWER_ON = 1,
-        LED_CTL_LINK = 2,
-        LED_CTL_NO_LINK = 3,
-        LED_CTL_TX = 4,
-        LED_CTL_RX = 5,
-        LED_CTL_SITE_SURVEY = 6,
-        LED_CTL_POWER_OFF = 7,
-        LED_CTL_START_TO_LINK = 8,
-        LED_CTL_START_WPS = 9,
-        LED_CTL_STOP_WPS = 10,
-        LED_CTL_START_WPS_BOTTON = 11, //added for runtop
-        LED_CTL_STOP_WPS_FAIL = 12, //added for ALPHA
-	 LED_CTL_STOP_WPS_FAIL_OVERLAP = 13, //added for BELKIN
-}LED_CTL_MODE;
+enum LED_CTL_MODE {
+	LED_CTL_POWER_ON = 1,
+	LED_CTL_LINK = 2,
+	LED_CTL_NO_LINK = 3,
+	LED_CTL_TX = 4,
+	LED_CTL_RX = 5,
+	LED_CTL_SITE_SURVEY = 6,
+	LED_CTL_POWER_OFF = 7,
+	LED_CTL_START_TO_LINK = 8,
+	LED_CTL_START_WPS = 9,
+	LED_CTL_STOP_WPS = 10,
+	LED_CTL_START_WPS_BOTTON = 11, //added for runtop
+	LED_CTL_STOP_WPS_FAIL = 12, //added for ALPHA
+	LED_CTL_STOP_WPS_FAIL_OVERLAP = 13, //added for BELKIN
+};
 
 
 //================================================================================
@@ -123,7 +123,7 @@ struct led_priv{
 	LED_871x			SwLed1;
 	LED_STRATEGY_871x	LedStrategy;
 	u8					bRegUseLed;
-	void (*LedControlHandler)(_adapter *padapter, LED_CTL_MODE LedAction);
+	void (*LedControlHandler)(_adapter *padapter, enum LED_CTL_MODE LedAction);
 	/* add for led controll */
 };
 
