From 1f14798657a1be2b7840d5fe69b40491871e96d8 Mon Sep 17 00:00:00 2001
From: Larry Finger <Larry.Finger@lwfinger.net>
Date: Sat, 15 Mar 2014 17:29:19 -0500
Subject: [PATCH 300/390] Remove unused CONFIG_NO_WIRELESS_HANDLERS

Signed-off-by: Larry Finger <Larry.Finger@lwfinger.net>
---
 os_dep/ioctl_linux.c | 5 -----
 os_dep/os_intfs.c    | 8 --------
 2 files changed, 13 deletions(-)

--- a/drivers/staging/rtl8192du/os_dep/ioctl_linux.c
+++ b/drivers/staging/rtl8192du/os_dep/ioctl_linux.c
@@ -7021,11 +7021,6 @@ int rtw_ioctl(struct net_device *dev, st
 	case RTL_IOCTL_HOSTAPD:
 		ret = rtw_hostapd_ioctl(dev, &wrq->u.data);
 		break;
-#ifdef CONFIG_NO_WIRELESS_HANDLERS
-	case SIOCSIWMODE:
-		ret = rtw_wx_set_mode(dev, NULL, &wrq->u, NULL);
-		break;
-#endif
 #endif
 	case (SIOCDEVPRIVATE+1):
 		ret = rtw_android_priv_cmd(dev, rq, cmd);
--- a/drivers/staging/rtl8192du/os_dep/os_intfs.c
+++ b/drivers/staging/rtl8192du/os_dep/os_intfs.c
@@ -935,10 +935,6 @@ _adapter *rtw_drv_add_vir_if (struct rtw
 	DBG_8192D("register rtw_netdev_virtual_iface_ops to netdev_ops\n");
 	pnetdev->netdev_ops = &rtw_netdev_vir_if_ops;
 
-#ifdef CONFIG_NO_WIRELESS_HANDLERS
-	pnetdev->wireless_handlers = NULL;
-#endif
-
 	/****** init adapter ******/
 	padapter = rtw_netdev_priv(pnetdev);
 	memcpy(padapter, primary_padapter, sizeof(struct rtw_adapter));
@@ -1270,10 +1266,6 @@ struct rtw_adapter *rtw_drv_if2_init(str
 	DBG_8192D("register rtw_netdev_if2_ops to netdev_ops\n");
 	pnetdev->netdev_ops = &rtw_netdev_if2_ops;
 
-#ifdef CONFIG_NO_WIRELESS_HANDLERS
-	pnetdev->wireless_handlers = NULL;
-#endif
-
 	/****** init adapter ******/
 	padapter = rtw_netdev_priv(pnetdev);
 	memcpy(padapter, primary_padapter, sizeof(struct rtw_adapter));
