From 388609ba5d52b6909f8b5eb53bc765b1f95df453 Mon Sep 17 00:00:00 2001
From: Stefan Lippers-Hollmann <s.l-h@gmx.de>
Date: Sat, 22 Mar 2014 03:03:08 +0100
Subject: [PATCH 410/456] rtl8192du: remove unused function rtw_division64()

Signed-off-by: Stefan Lippers-Hollmann <s.l-h@gmx.de>
---
 include/osdep_service.h | 3 ---
 os_dep/osdep_service.c  | 6 ------
 2 files changed, 9 deletions(-)

--- a/drivers/staging/rtl8192du/include/osdep_service.h
+++ b/drivers/staging/rtl8192du/include/osdep_service.h
@@ -334,11 +334,8 @@ void rtw_free_netdev(struct net_device *
 #define rtw_signal_process(pid, sig) kill_pid(find_vpid((pid)),(sig), 1)
 
 u64 rtw_modular64(u64 x, u64 y);
-u64 rtw_division64(u64 x, u64 y);
-
 
 /* Macros for handling unaligned memory accesses */
-
 #define RTW_GET_BE16(a) ((u16) (((a)[0] << 8) | (a)[1]))
 #define RTW_PUT_BE16(a, val)			\
 	do {					\
--- a/drivers/staging/rtl8192du/os_dep/osdep_service.c
+++ b/drivers/staging/rtl8192du/os_dep/osdep_service.c
@@ -341,12 +341,6 @@ u64 rtw_modular64(u64 x, u64 y)
 	return do_div(x, y);
 }
 
-u64 rtw_division64(u64 x, u64 y)
-{
-	do_div(x, y);
-	return x;
-}
-
 void rtw_buf_free(u8 **buf, u32 *buf_len)
 {
 	if (!buf || !buf_len)
