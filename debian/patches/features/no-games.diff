--- a/kernel/module.c
+++ b/kernel/module.c
@@ -2575,6 +2575,13 @@ static struct module *load_module(void _
 	if (err)
 		goto free_unload;
 
+	/*
+	 * no games, call it what it is
+	 * even if "certain" tools claim otherwise
+	 */
+	if (strcmp(mod->name, "fglrx") == 0)
+		add_taint_module(mod, TAINT_PROPRIETARY_MODULE);
+
 	/* Set up MODINFO_ATTR fields */
 	setup_modinfo(mod, &info);
 
